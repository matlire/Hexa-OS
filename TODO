Input: ""; Action: ""; Output/Error: ""; Comment: ""
Bugs:
1) Fix home/end buttons to jump to start/end of line, not full input (maybe leave as no bug but feature?;> )

Add comments, do docs!

Major tasks:
1) PIT
2) RTC
3) memory management, heap (malloc, free, etc)
4) APIC
5) HPET
6) process control block
7) scheduler
8) user mode
9) syscalls
Filesystem
...
And much more;)

Minor tasks:
1) full kb support:
	- numpad
	- F1-F12 buttons
2) docs
3) Add commands: shutdown, restart

Refactor:
1) comments (!)
