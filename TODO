Input: ""; Action: ""; Output/Error: ""; Comment: ""
Bugs:
1) Fix home/end buttons to jump to start/end of line, not full input (maybe leave as no bug but feature?;> )

Add comments, do docs!

Major tasks:
1) IRQ0
2) memory management, heap (malloc, free, etc)
3) process control block
4) scheduler
5) user mode
6) syscalls
Filesystem
...
And much more;)

Minor tasks:
1) full kb support:
	- numpad
	- F1-F12 buttons
2) docs

Refactor:
1) comments (!)
